from src.paper.bounded_metric_delta_behaviour import main as bounded_metric_delta_behaviour
from src.paper.constant_dist_over_size import main as constant_dist_over_size
from src.paper.linear_error_increase import main as linear_error_increase
from src.paper.minimal_dataset_averaging import main as minimal_dataset_averaging

if __name__ == '__main__':
    bounded_metric_delta_behaviour()
    constant_dist_over_size()
    linear_error_increase()
    minimal_dataset_averaging()
